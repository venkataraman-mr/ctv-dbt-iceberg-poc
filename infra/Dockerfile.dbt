FROM python:3.11-slim
RUN apt-get update && apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir dbt-trino==1.10.1
WORKDIR /dbt
# dbt-trino pulls dbt Core as a dependency. Project is bind-mounted at /dbt.
