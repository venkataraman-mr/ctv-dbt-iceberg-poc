# pin what you validate on the VM
pyiceberg[pyarrow]==0.11.1
# Pin pyarrow: pyiceberg 0.11.1 only caps pyarrow at ">=17.0.0" (no upper bound), but its
# io.pyarrow module fails to import on pyarrow 25 (which deltalake 0.25.5 pulls by default).
# When that import fails, PyIceberg silently falls back to the fsspec FileIO -> needs s3fs.
# 20.0.0 satisfies deltalake (>=16,!=19) AND is a version pyiceberg 0.11.1 was tested against.
pyarrow==20.0.0
deltalake==0.25.5
# 1.5.x (current stable) — future-proofs the runtime delta/azure extension downloads (the repo
# eventually drops very old versions). 1.1.3 also worked; bumped only for longevity.
duckdb==1.5.5
# CTV landing (Piece 1): exact Spark xxhash64(seed 42) for creative_url_hash. Verified against
# real PySpark 3.5 xxhash64; Trino's built-in (seed 0) cannot reproduce it, so we precompute here.
xxhash==3.5.0
